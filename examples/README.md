# Examples — 端到端示例

> 展示如何用本 playbook 的 skills 驱动一个真实任务走完五层闭环。

| 示例 | 场景 | 文件 |
|---|---|---|
| 旅游助手前端现代化 | LangChain+Qwen+Gradio 项目的前端重构 | `travel-frontend-refactor.md` |

每个示例对照五层（Plan → Execute → Verify → Observe → Improve），标注每步调用了哪个 skill、产出什么。示例中的 Spec/Issue 为演示样例，实际执行时由对应 skill 生成。
